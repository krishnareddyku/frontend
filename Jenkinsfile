pipeline {
	agent {
	label 'kk'
	}
	
	stages {
		stage('compile/build') {
			steps {
			echo "compile"
			}
		}
		stage('Unit tests') {
                        steps {
                        echo "unit tests"
                        }
                }
	}

}
